pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'mvn clean package'
                sh 'scp webapp.war 192.168.1.77:/home/anu'
            }
        }
    }
    post { 
        always { 
            cleanWs()
        }
    }
}
